<html>
<head>
    <title>Inscription</title>
    <link rel="stylesheet" href="{{ asset('css/bootstrap.css') }}">
    <link rel="stylesheet" href="{{ asset('css/style_florian.css') }}">
    <link href="https://use.fontawesome.com/releases/v5.0.7/css/all.css" rel="stylesheet">
</head>
<body>
<a href="{{ path('login') }}" class="left-btn btn btn-danger"><i class="fas fa-arrow-left"></i> Retour</a>
{{ form_start(form) }}
{{ form_errors(form) }}
<div class="background">
    <div class="container">
            <div class="center-form">
                <div class="jumbotron">
                    {{ form_label(form) }}
                    {{ form_errors(form) }}
                    {{ form_widget(form) }}
                </div>
            </div>
    </div>
</div>
{{ form_end(form) }}
</body>
</html>